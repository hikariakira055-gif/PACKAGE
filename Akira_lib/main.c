#include "include/Vector2D.h"
#include "include/Vector3D.h"
#include "include/Vector.h"
#include "include/char_chain.h"
#include "include/mathlab.h"
#include "include/akr55.h"
#include "include/tensor.h"

void __start(void)
{
    // Vector all;
    //String string;


    // cat(&string, "Hello");
    // cat(&string, "World");
    // cat(&string, "Kernel");
    // cat(&string, "Aull0");
    // cat(&string, "Bull1");
    // cat(&string, "Full2");
    // cat(&string, "Null3");
    // cat(&string, "Mull4");
    
    // dico(&string, "az");
    // printf("%s", string.string[0]);
    
    // char *string__ = "hello:world:Iam:who:Iam";
    
    // sep(string__, ':');
    // &string, 

    Layers FIRST;
    init_layer(3, &FIRST);

   
}

int main(void)
{
    
    __start();
    return (0);

    //find . -type f -name "*.c" -o -name "*.h" | xargs wc -l

}